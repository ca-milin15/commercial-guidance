spring:
  application:
    name: commercial-guidance

server:
  port: 8091

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
